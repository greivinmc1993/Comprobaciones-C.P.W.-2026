
/* VARIABLES CSS */
:root{
    --color-tinta: ; /*  */
    --color-suave: ; /*  */
    --color-marca: ; /*  */
    --color-acento: ; /*  */
    --color-acento-osc: ; /* */
    --color-fondo: linear-gradient(135deg, #5a2024, #a64943); /* */
    --color-fondo-alt: ; /* */
    --color-linea: ; /* */
    --color-blanco: ; /* */
    --fondo: radial-gradient(circle at 80% 25%, rgba(197, 139, 80, 0.55), transparent 27%), radial-gradient(circle at 20% 80%, rgba(49, 92, 74, 0.55), transparent 30%), linear-gradient(120deg, rgb(53, 28, 29), rgb(104, 40, 40) 48%, rgb(49, 92, 74));
    --fondo-2: linear-gradient(rgba(53, 28, 29, 0.9), rgba(53, 28, 29, 0.9)), radial-gradient(circle, #a85c4d, #351c1d);

/* FUENTES Y TITULOS */
    --fuente-titulo: Georgia, "Times New Roman", serif;
    --fuente-texto: "Trebuchet MS", Arial, sans-serif;

/* ESPACIADO CONVERSION 1REM=10PX*/
    --esp-xs: 0.8rem;
    --esp-sm: 1.6rem;
    --esp-md: 2.4rem;
    --esp-lg: 4.0rem;
    --esp-xl: 8.0rem;

/* OTROS ELEMENTOS */
    --radio: 0.6rem;
    --sombra-1: 0 0.2rem 0.6rem rgba(0,0,0,0.6);/* APENAS SEPARADO */
    --sombra-2: 0 0.4rem 1.2rem rgba(0,0,0,0.10);/* TARJETAS */
    --sombra-3: 0 0.8rem 2.0rem rgba(0,0,0,0.16);/* ELEMENTOS ELEVADOS*/
    --transition: all 0.25s ease;
    --ancho-max: 120rem;

/* GRID FLEX*/
    --grid: repeat(auto-fit, minmax(28rem, 1fr));/* Pon tantas columnas como quepan → mínimo 28rem → y reparte el espacio sobrante. */
    --flex: flex: 1 1 32rem; /* "Quiero empezar en 32rem, puedo crecer si sobra espacio y puedo encogerme si falta espacio." */
}

*{
    padding: 0px;
    margin: 0px;
    box-sizing: border-box; /* SIN ELLA LAS MEDIDAS DE UNA CAJA ES IGUAL A LA MEDIDA DE TODO (PADDING, MARGIN, BORDER) */
}

/* LA BASE DEL TAMAÑO */
html{
    font-size: 62.5%;
    scroll-behavior: smooth;
}

body{
    font-size: 1.6rem;
    font-family: var(--fuente-texto);
    line-height: 1.6;
    color: var(--color-tinta);
    background-color: var(--color-fondo);
}

img{
    max-width: 100%;
    height: auto;
    display: block;
}

/* JERARQUIA DE TITULOS */
h1, h2, h3{
    font-family: var(--fuente-titulo);
    line-height: 1.2;
    color: var(--color-marca);
}

h1{
    font-size: 4.8rem;
}

h2{
    font-size: 3.2rem;
}

h3{
    font-size: 2.0rem;
}

p{
    margin-bottom: var(--esp-sm);
}

a{
    color: var(--color-acento);
    text-decoration: none;
}

/* CONTENEDORES Y SECCIONES */


/*CABECERA Y MENU*/
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: var(--esp-sm) var(--esp-md);
    border-bottom: 0.1rem solid var(--color-linea);
    position: sticky;
    top: 0;
    z-index: 100;
    background-color: var(--color-fondo);
    box-shadow: var(--sombra-1);
}

.logo {
    display: flex;
    align-items: center;
    gap: var(--esp-xs);
    color: var(--color-marca);
}

.logo:hover .logo-icono {
    transform: rotate(-8deg);
}

.logo-texto {
    font-family: var(--fuente-titulo);
    font-size: 2rem;
    font-weight: 700;
}

.nav-flex {
    display: flex;
    gap: var(--esp-sm);
    list-style: none;
}

.nav-enlace {
    display: inline-block; /*esta linea perimte aplicar relleno vertical*/
    padding: var(--esp-xs) var(--esp-sm);
    color: var(--color-tinta);
    font-weight: 600;
    border-radius: var(--radio);
    transition: background-color var(--transicion), color var(--transicion);
}

.menu-enlace:hover {
    background-color: var(--color-fondo-alt);
    color: var(--color-marca);
}

.menu-enlace:focus {
    outline: 0.2rem solid var(--color-acento);
    outline-offset: 0.2rem;
}

@keyframes resaltar{
    0%{
        transform: translateY(0px) ;
    }
    50%{
        transform: translateY(-5px) ;
    }
    100%{
        transform: translateY(0px) ;
    }
}
